from random import randint

from autofixture import AutoFixture

from apps.ngov.models import Office

# fixture = AutoFixture(Office, field_values={'needed_players': randint(2,10)})
# entries=fixture.create(30)